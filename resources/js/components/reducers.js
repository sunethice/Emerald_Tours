import { CREATE_TODO } from './actionTypes';

export const todos = (state, action) => {
    const { type, payload } = action;

    switch(type){
        case '':
            return;
        case '':
            return;
        default:
            return state;
    }
}
